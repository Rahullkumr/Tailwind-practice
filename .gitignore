# Ignore node_modules directory
node_modules/

# Ignore the output CSS files (if they are generated and don't need to be tracked)
dist/
src/output.css

# Ignore logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore environment variable files (if you're using any)
.env
.env.local
.env.*.local

# Ignore lock files (optional, depends on your team preferences)
# You can choose either package-lock.json or yarn.lock based on the package manager you're using
package-lock.json
yarn.lock

# Ignore macOS-specific files
.DS_Store

# Ignore build artifacts and cache
build/
.cache/
